# 修复控制台错误

## 1. 修复 `next/image` 域名配置问题
在 `next.config.mjs` 中添加 `via.placeholder.com` 域名配置，以支持项目中可能使用的占位图服务：
```javascript
{
  protocol: 'https',
  hostname: 'via.placeholder.com',
}
```

## 2. 验证修复
配置更新后，开发服务器需要重启以使配置生效。检查控制台确认错误是否消失。

## 影响范围
- 修改文件：`frontend/next.config.mjs`
- 无需修改任何组件代码

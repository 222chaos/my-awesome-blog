## 实施计划：将首页文章改为从后端获取热门文章

### 当前状态
- **后端接口**：`GET /api/v1/articles/popular?limit=N` (articles.py:87-103)
- **前端服务**：`getPopularArticles(limit)` 已存在 (articleService.ts:106-113)
- **类型定义**：`Article` 类型已定义 (types/index.ts:212-255)
- **当前问题**：StatsPanel.tsx 使用硬编码的 mockArticles

### 需要修改的文件

#### 1. `frontend/src/components/home/StatsPanel.tsx`

**修改内容**：
- 移除 mockArticles 数组（第64-95行）
- 在组件中添加 `useState` 存储从 API 获取的文章
- 在 `useEffect` 中调用 `getPopularArticles(3)` 获取前3篇热门文章
- 适配后端 Article 类型到组件需要的格式：
  - `published_at` → `date`
  - `likes_count` → `likes`
  - `comments_count` → `comments`
  - `cover_image` → `image`
  - `categories[0].name` → `category`

**保留功能**：
- 现有的加载状态处理
- 错误处理和重试逻辑
- 图片加载失败的 fallback（显示 avatar.jpg）
- 动画延迟效果

### 优势
1. 展示真实的热门文章（基于浏览量排序）
2. 动态内容，文章更新后自动反映
3. 复用现有的 API 和类型定义
4. 保持良好的用户体验（加载、错误状态）
## 修复计划

### 1. 删除未使用的组件 (节省约 700 行代码)
- **删除** `frontend/src/components/home/ArticleCardSkeleton.tsx` (23行) - 无任何导入
- **删除** `frontend/src/components/messages/Danmaku.tsx` (154行) - 基础版本未使用
- **删除** `frontend/src/components/messages/OptimizedDanmaku.tsx` (525行) - 未被使用（messages 页面使用 EnhancedDanmaku）

### 2. 统一 token 存储键名
- **修改** `frontend/src/services/llmService.ts` 第30行：`access_token` → `auth_token`
- **修改** `frontend/src/services/conversationService.ts` 第84行：`access_token` → `auth_token`
- 原因：其他14+个服务文件都使用 `auth_token`，保持一致性

### 3. 统一 API URL 格式
- **修改** `frontend/src/services/llmService.ts` 第31行：`127.0.0.1:8989` → `localhost:8989`
- **修改** `frontend/src/services/conversationService.ts` 第85行：`127.0.0.1:8989` → `localhost:8989`
- 原因：与 `lib/api/llm.ts` 保持一致

### 不合并的组件
- 保留 `/chat` 和 `/ai/chat` 的独立实现（localStorage vs API，用途不同）
- 保留独立的 ChatInput、ChatSidebar、MessageBubble（功能差异大）
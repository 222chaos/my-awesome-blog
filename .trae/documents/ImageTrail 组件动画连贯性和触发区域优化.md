# ImageTrail 组件优化方案

## 1. 动画连贯性优化
- 降低 threshold 从 50 到 20-30 像素，使图片触发更频繁
- 增加 lerp 系数从 0.1 到 0.15-0.2，减少延迟感
- 优化动画时序，使过渡更平滑

## 2. 触发区域优化
- 移除重复的事件监听器绑定
- 添加精确的触发区域检测，只在鼠标移动时才激活
- 优化容器大小和事件处理范围

## 3. 具体修改
- 修改所有 8 个 Variant 类的 threshold 值和 lerp 系数
- 重构事件监听器绑定逻辑
- 优化 render 循环中的距离检测逻辑
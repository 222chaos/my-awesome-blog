## 修复计划

### 1. 修复 Next.js 配置
- 将 `experimental.typedRoutes` 更新为 `typedRoutes`（已存在，只需删除警告的配置）
- 移除不必要的 webpack 配置（因为使用 Turbopack）

### 2. 检查并修复图片路径
- 确保所有文章使用 OSS URL 而不是本地 `/assets/` 路径
- 检查 `HoloCard` 组件如何处理 `featured_image` 和 `cover_image`

### 3. 检查 @vite/client 404 问题
- 检查是否有组件或依赖引入了 Vite 相关代码
- 清理可能的依赖或配置问题

### 4. 重启前端服务器
- 重启以应用配置更改
- 验证错误是否解决

### 5. 测试文章页面导航
- 验证点击文章链接是否正常工作
- 检查封面图是否正确显示
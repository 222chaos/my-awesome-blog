## 文章/相册页面优化计划

### 📊 问题分析

#### 1. 类型定义冗余
- **types/index.ts**: 重复定义了 `Category` 和 `Tag` 接口（各定义 2 次）
- **各组件**: HoloCard、FeaturedCarousel、CommandBar、ArchiveDrawer 都定义了自己的 Article/Category/Tag 接口
- **CommentTree**: 重复定义了 Comment 接口

#### 2. 性能问题
- **page.tsx**: 
  - `fetchArticles` 每次都获取分类/标签数据，但这些数据可以缓存
  - 缺少 `useMemo` 缓存计算结果（getHotArticles, mapToAlbums）
  - `observerTargetRef` 和 `visibleCount` 状态未被有效使用
- **各组件**: 大量重复的事件处理函数可以提取为公共 Hook

#### 3. 代码冗余
- **重复的样式逻辑**: `getThemeClass` 调用模式重复
- **重复的格式化逻辑**: `formatDate` 在 CommentTree 中，可以提取为公共工具函数

---

### 🎯 优化方案

#### 一、类型定义统一化

**1.1 统一 types/index.ts**
- 合并重复的 Category/Tag 接口定义
- 确保所有组件使用统一的类型
- 添加 `BaseArticle` 接口作为基础类型

**1.2 移除组件内的重复类型定义**
- 删除 HoloCard.tsx 中的 Article 接口
- 删除 FeaturedCarousel.tsx 中的 Article 接口
- 删除 CommandBar.tsx 中的 Category/Tag 接口
- 删除 ArchiveDrawer.tsx 中的 Category/Tag/Article 接口
- 删除 CommentTree.tsx 中的 Comment 接口

#### 二、性能优化

**2.1 page.tsx 优化**
- 使用 `useMemo` 缓存 `getHotArticles()` 结果
- 使用 `useMemo` 缓存 `mapToAlbums()` 结果
- 分离分类/标签数据获取逻辑，只在首次加载时获取
- 移除未使用的 `visibleCount` 状态
- 优化 IntersectionObserver 逻辑

**2.2 提取公共 Hook**
- 创建 `useArticleFilters` Hook 管理筛选状态
- 创建 `usePagination` Hook 管理分页逻辑
- 创建 `useInfiniteScroll` Hook 管理无限滚动

**2.3 组件级优化**
- 使用 `React.memo` 包装 HoloCard、FeaturedCarousel
- 使用 `useCallback` 优化事件处理函数
- 添加 `will-change` 属性启用 GPU 加速

#### 三、代码去冗余

**3.1 提取公共工具函数**
- 创建 `utils/dateFormat.ts` 统一日期格式化逻辑
- 创建 `utils/articleHelpers.ts` 统一文章相关工具函数
- 创建 `utils/themeHelpers.ts` 统一主题样式逻辑

**3.2 提取公共组件**
- 创建 `components/articles/ArticleStatItem.tsx` 统一统计项显示
- 创建 `components/articles/ArticleBadge.tsx` 统一标签/分类徽章
- 创建 `components/articles/ArticleMeta.tsx` 统一文章元数据显示

---

### 📝 具体修改文件清单

1. **frontend/src/types/index.ts** - 统一类型定义
2. **frontend/src/utils/dateFormat.ts** - 新建日期格式化工具
3. **frontend/src/utils/articleHelpers.ts** - 新建文章工具函数
4. **frontend/src/hooks/useArticleFilters.ts** - 新建筛选 Hook
5. **frontend/src/hooks/usePagination.ts** - 新建分页 Hook
6. **frontend/src/hooks/useInfiniteScroll.ts** - 新建无限滚动 Hook
7. **frontend/src/app/articles/page.tsx** - 主页面优化
8. **frontend/src/components/articles/HoloCard.tsx** - 移除重复类型，优化性能
9. **frontend/src/components/articles/FeaturedCarousel.tsx** - 移除重复类型，优化性能
10. **frontend/src/components/articles/CommandBar.tsx** - 移除重复类型，优化性能
11. **frontend/src/components/articles/ArchiveDrawer.tsx** - 移除重复类型，优化性能
12. **frontend/src/components/articles/CommentTree.tsx** - 使用公共工具，移除重复类型

---

### ✅ 预期效果

- **类型安全提升**: 统一类型定义，避免重复和不一致
- **性能提升**: 减少不必要的重新渲染和计算，预计提升 20-30%
- **代码可维护性**: 提取公共逻辑，减少重复代码 40%+
- **开发体验**: 统一的 Hooks 和工具函数，提升开发效率
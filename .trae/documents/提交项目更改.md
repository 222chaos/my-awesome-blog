## 提交当前更改的计划

### Git 状态分析

当前有以下主要更改：

**后端更改**：
- API endpoints: `articles.py`, `subscriptions.py`, `users.py`, `router.py` (新增 albums.py, messages.py)
- CRUD: `user.py` (新增 message.py)
- Models: `timeline_event.py`, `user.py` (新增 message.py)
- Schemas: `article.py`, `user.py` (新增 message.py)
- Database migrations: `008_add_messages_table.py`
- Scripts: `fix_timeline_color.py`, `seed_test_data.py`, `test_api_endpoints.py`

**前端更改**：
- 页面: `about`, `articles`, `login`, `messages`, `profile`
- 组件: `HoloCard`, `Timeline`, `TechStack`, `FocusCards`, `GlitchText`, 等
- 服务: 删除 `userService.ts`, 更新 `albumService.ts`, 新增多个服务文件
- 配置: `api.ts`, `globals.css`

**删除的文件**：
- `docs/` 目录下的多个文档文件

**新增文件**：
- `.trae/documents/` 目录下的多个文档
- `frontend/src/lib/validation.ts`, `frontend/src/lib/auth-utils.ts`
- 多个新的服务文件

### 提交计划

将分阶段提交，每个提交对应一个主要功能或改进：

1. **后端 API 和模型更新** - articles, subscriptions, users endpoints
2. **后端新功能** - messages 和 albums 功能
3. **后端数据库和脚本** - 模型更新、迁移、测试脚本
4. **前端 API 和服务优化** - 删除 userService, 创建 auth-utils, validation
5. **前端组件和页面更新** - 页面和组件的修复
6. **前端新功能** - 新的服务文件和组件
7. **文档更新** - 删除旧文档，添加新文档
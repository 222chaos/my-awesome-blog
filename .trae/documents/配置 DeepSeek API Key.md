## 前端项目文件整理计划

### 📋 发现的问题

#### 1. 未使用的组件

* **`RankingCard.tsx`** - 已定义但从未在 page.tsx 中使用

* **`media-player-test/page.tsx`** - 仅用于测试的页面，可删除

#### 2. 重复的卡片组件

* `ui/card.tsx` - 通用卡片

* `ui/GlassCard.tsx` - 玻璃态卡片

* `ui/HoloCard.tsx` - 全息卡片

* `ui/NeonCard.tsx` - 霓虹卡片

* `ui/PostCard.tsx` - 文章卡片（与 `blog/SimplePostCard.tsx` 重复）

* `ui/FocusCards.tsx` - 聚焦卡片

* `ui/FriendLinkCard.tsx` - 友链卡片

#### 3. 类型定义重复

* `types/music.ts` - 音乐相关类型

* 各组件内部定义的类型（如 albums/page.tsx 中的 Album 接口）

#### 4. 样式文件

* `login/form-styles.css` - 登录页面专用样式，可内联到组件中

***

### 🛠 整理方案

#### 第一阶段：清理冗余文件

1. 删除 `components/music/RankingCard.tsx`（未使用）
2. 删除 `app/posts/media-player-test/page.tsx`（测试页面）

#### 第二阶段：组件整理

1. 评估卡片组件用途，制定合并/保留策略：

   * `GlassCard` - 保留（玻璃态设计）

   * `HoloCard` - 保留（全息特效）

   * `card.tsx` - 保留（Radix UI 基础卡片）

   * `PostCard` - 删除（已被 `SimplePostCard` 替代）

   * `NeonCard` - 评估是否被使用

   * `FocusCards` - 评估是否被使用

   * `FriendLinkCard` - 评估是否与 `FriendLinks.tsx` 重复

#### 第三阶段：类型统一

1. 将重复的类型定义移至 `types/` 目录：

   * albums/page.tsx 的 `Album` 接口 → types/album.ts

   * 统一类型导出

#### 第四阶段：样式整理

1. 将 `login/form-styles.css` 内联到组件或移至 `styles/` 目录

#### 第五阶段：目录优化

1. 确保组件目录结构清晰：

   * `components/ui/` - 基础 UI 组件

   * `components/music/` - 音乐组件

   * `components/home/` - 首页组件

   * 避免交叉引用

***

### ✅ 预期效果

* 减少约 5-10 个冗余文件

* 组件职责更清晰

* 类型定义统一管理

* 代码库更易维护


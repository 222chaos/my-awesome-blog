## 检查结果与修复计划

### 🚨 严重问题

**误删正在使用的组件**
- 错误删除了 `components/home/ArticleCardSkeleton.tsx`，但 `StatsPanel.tsx` 正在导入使用它
- 这会导致首页统计数据板块加载失败
- 需要恢复该文件

### ⚠️ 潜在问题

**1. 环境变量系统不统一**
- `llmService.ts` 和 `conversationService.ts` 使用 `import.meta.env.VITE_*` (Vite 语法)
- `websocketService.ts` 使用 `process.env.NEXT_PUBLIC_*` (Next.js 语法)
- 项目使用 Next.js，应统一使用 `process.env.NEXT_PUBLIC_*`

**2. API URL 格式不一致**
- `llmService.ts` 和 `conversationService.ts` 使用 `localhost:8989`
- `websocketService.ts` 使用 `127.0.0.1:8989`
- 建议统一为 `localhost:8989`

**3. 已修复的项（验证通过）**
- ✅ `access_token` 已全部替换为 `auth_token`（无残留）
- ✅ Danmaku 相关组件删除正确（messages/page.tsx 使用 EnhancedDanmaku）
- ✅ OptimizedDanmaku.tsx 确实未被使用

## 修复计划

1. **恢复 ArticleCardSkeleton.tsx** - 重新创建 `components/home/ArticleCardSkeleton.tsx`
2. **统一环境变量** - 将 `import.meta.env.VITE_API_BASE_URL` 改为 `process.env.NEXT_PUBLIC_API_BASE_URL`
3. **统一 API URL** - websocketService.ts 的 `127.0.0.1:8989` 改为 `localhost:8989`
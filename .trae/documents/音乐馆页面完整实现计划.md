## 🎯 音乐馆页面实现计划

基于优化后的macOS风格设计文档，完整实现音乐馆页面。

### 📋 第一阶段：Tailwind配置更新

**更新 `tailwind.config.js`:**
- 添加SF Pro字体配置（使用系统字体fallback）
- 添加macOS圆角系统（rounded-lg=12px, rounded-xl=16px, rounded-2xl=24px, rounded-3xl=32px）
- 添加Apple Music主色调（#fa2d2f）
- 添加macOS系统灰度颜色
- 添加macOS标准阴影系统（shadow-1到shadow-5）
- 添加macOS缓动曲线（ease-out, ease-in-out, spring, custom-a, custom-b）
- 添加macOS动画时长（150-600ms）
- 添加macOS动画延迟（delay-50到delay-500）
- 添加SF Pro字体大小（title-1到caption-2）
- 添加自定义间距（w-65=260px, w-70=280px, h-22=88px）

### 📋 第二阶段：类型定义

**创建 `frontend/src/types/music.ts`:**
- Song接口（歌曲）
- Playlist接口（歌单）
- Artist接口（歌手）
- Album接口（专辑）
- Banner接口（轮播图）
- PlayMode类型
- PlayerState接口

### 📋 第三阶段：组件创建

**3.1 侧边栏组件**
- `frontend/src/components/music/MusicSidebar.tsx`
- 260px标准宽度，支持折叠
- 导航项44px高度，12px圆角
- 选中状态左侧红色指示条
- 支持响应式（移动端隐藏）

**3.2 Hero Banner组件**
- `frontend/src/components/music/HeroBanner.tsx`
- 360px标准高度，16px圆角
- 3D倾斜封面效果
- 轮播指示器
- 自动播放5秒切换
- 悬停显示切换箭头

**3.3 歌单卡片组件**
- `frontend/src/components/music/PlaylistCard.tsx`
- 16px圆角，8px间距
- 悬停滑入播放按钮
- 播放数标签

**3.4 歌曲列表组件**
- `frontend/src/components/music/SongList.tsx`
- `frontend/src/components/music/SongRow.tsx`
- 56px行高
- 当前播放红色指示条
- 音波动画图标
- 悬停显示操作按钮

**3.5 排行榜样板**
- `frontend/src/components/music/RankingCard.tsx`
- 24px圆角
- 显示前3首歌曲

**3.6 歌手卡片**
- `frontend/src/components/music/ArtistCard.tsx`
- 圆形头像
- 悬停轻微放大

**3.7 底部播放器**
- `frontend/src/components/music/PlayerBar.tsx`
- 88px高度，固定底部
- 三段布局（30%-40%-30%）
- 进度条设计
- 播放控制按钮
- 音量控制
- 展开功能

**3.8 辅助组件**
- `frontend/src/components/music/Section.tsx`（区块标题）
- `frontend/src/components/music/ProgressBar.tsx`（进度条）
- `frontend/src/components/music/VolumeControl.tsx`（音量控制）
- `frontend/src/components/music/MobileNav.tsx`（移动端底部导航）

**3.9 横向滚动组件**
- `frontend/src/components/music/PlaylistScroll.tsx`
- `frontend/src/components/music/ArtistScroll.tsx`

### 📋 第四阶段：页面布局

**创建 `frontend/src/app/music/page.tsx`:**
- 三栏布局（侧边栏+主内容+播放器）
- 响应式适配
- Hero Banner区域
- 推荐歌单区域
- 排行榜样板区域
- 热门歌手区域
- 最新音乐区域

**创建 `frontend/src/app/music/layout.tsx`:**
- 音乐馆专属布局
- 固定播放器栏
- 响应式导航切换

### 📋 第五阶段：工具函数

**更新 `frontend/src/lib/utils.ts`:**
- 添加时间格式化函数（formatTime）
- 添加播放数格式化函数（formatPlayCount）
- 添加时长格式化函数（formatDuration）

### 📋 第六阶段：样式优化

**创建 `frontend/src/app/music/globals.css`:**
- macOS特定样式
- 自定义滚动条
- 3D效果样式

### 📋 第七阶段：图标资源

确保使用Heroicons/Lucide图标，避免emoji：
- PlayIcon, PauseIcon
- BackwardIcon, ForwardIcon
- ShuffleIcon, RepeatIcon
- HeartIcon, HeartFillIcon
- ListIcon, SearchIcon
- SpeakerIcon, MuteIcon
- ChevronLeftIcon, ChevronRightIcon, ChevronUpIcon

### 📋 第八阶段：响应式适配

- 移动端（<768px）：隐藏侧边栏，底部导航
- 平板（768-1024px）：侧边栏可折叠
- 桌面（>1024px）：完整布局

### 📋 第九阶段：测试验证

- Light Mode和Dark Mode切换测试
- 响应式断点测试
- 交互动画测试
- 可访问性测试（键盘导航、ARIA标签）

### 📂 文件结构

```
frontend/src/
├── app/
│   └── music/
│       ├── layout.tsx
│       ├── page.tsx
│       └── globals.css
├── components/
│   └── music/
│       ├── MusicSidebar.tsx
│       ├── HeroBanner.tsx
│       ├── PlaylistCard.tsx
│       ├── PlaylistScroll.tsx
│       ├── SongList.tsx
│       ├── SongRow.tsx
│       ├── RankingCard.tsx
│       ├── ArtistCard.tsx
│       ├── ArtistScroll.tsx
│       ├── PlayerBar.tsx
│       ├── Section.tsx
│       ├── ProgressBar.tsx
│       ├── VolumeControl.tsx
│       └── MobileNav.tsx
├── types/
│   └── music.ts
└── lib/
    └── utils.ts (更新)
```

### ✅ 预期成果

- 完整的macOS风格音乐馆页面
- 遵循Apple Human Interface Guidelines
- 响应式设计，支持移动端
- 完整的播放器功能UI
- 流畅的动画和交互
## 修复消息页面内部服务器错误

### 1. 检查并运行种子数据脚本
- 运行 `seed_test_data.py` 确保数据库有测试留言数据
- 如果已有数据，则跳过

### 2. 添加调试日志
- 在 `/api/v1/messages/trending` 端点添加日志输出
- 在 `/api/v1/messages/stats/activity` 端点添加日志输出
- 记录查询结果和序列化过程

### 3. 修复 Schema 问题（如果需要）
- 检查 `MessageWithAuthor` 是否正确包含 User 的所有字段
- 确保 `get_message_activity` 返回的格式正确

### 4. 测试 API 端点
- 测试 `/api/v1/messages/trending?limit=5`
- 测试 `/api/v1/messages/stats/activity?days=7`
- 确认返回正确的 JSON 响应

## 预期结果
- 消息页面能够正常加载
- 热门留言和活跃度统计正确显示
- 不再出现 Internal Server Error